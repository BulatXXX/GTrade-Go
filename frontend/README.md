# Frontend (заглушка)

Frontend на Angular пока не реализован.

Подготовленная структура:

- `src/app/`

Следующие шаги:

1. Инициализировать Angular workspace в `frontend/`.
2. Добавить API-клиент к `api-gateway`.
3. Реализовать экраны auth/watchlist/catalog/notifications.
