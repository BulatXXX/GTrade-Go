# catalog-importer

CLI-заглушка для импорта каталога из внешних источников.

## Использование

```bash
go run ./cmd/catalog-importer -source warframe
```

Поддерживаемые значения `-source`:

- `warframe`
- `eve`
- `tarkov`
